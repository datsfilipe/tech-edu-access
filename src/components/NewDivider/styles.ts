import styled from 'styled-components'

export const Container = styled.div`
  width: 100%;
  .divider {
    width: 8rem;
    height: 1px;
    margin: 1.5rem auto;
    border-top: 1px solid var(--line);
  }
`